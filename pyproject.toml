[project]
name = "compute"
version = "0.1.0"
description = "Task and compute job management microservice"
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [
    {name = "Ananda Sarangaram"}
]
keywords = ["tasks", "compute", "jobs", "fastapi", "worker"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "alembic>=1.17.2",
    "basedpyright>=1.36.2",
    "cl-ml-tools @ git+https://github.com/cloudonlanapps/cl_ml_tools.git@main",
    "fastapi>=0.127.0",
    "loguru>=0.7.3",
    "pydantic>=2.12.5",
    "python-jose[cryptography]>=3.5.0",
    "sqlalchemy>=2.0.45",
    "uvicorn>=0.40.0",
]

[project.scripts]
compute-migrate = "compute.migrate:main"
compute-server = "compute.compute_server:main"
compute-worker = "compute.compute_worker:main"


# Plugin Registration
# Compute task plugins are auto-discovered from entry points.
# cl_ml_tools package provides built-in plugins. To add custom plugins,
# register them here:
#
# [project.entry-points."cl_ml_tools.tasks"]
# my_custom_task = "my_package.tasks:MyCustomTask"
#
# The worker will automatically discover and load all registered plugins.

[project.optional-dependencies]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "pytest-asyncio>=1.3.0",
]
all = [
    "compute[dev]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/compute"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
python_files = ["test_*.py"]
asyncio_mode = "strict"
markers = [
    "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
    "intelligence: marks tests related to intelligence features",
    "admin_only: marks tests as requiring admin permissions (deselect with '-m \"not admin_only\"')",
]
addopts = "--cov=compute --cov-report=html --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
source = ["src/compute"]
omit = [
    "*/tests/*",
    "*/__pycache__/*",
    "*/venv/*",
    "*/.venv/*",
    "*/compute_server.py",  # CLI entry point, not tested
    "*/compute_worker.py",  # CLI entry point, not tested
    "*/migrate.py",  # Migration utility script, not tested
    "*/utils.py",  # CLI startup utilities, tested manually
]

[tool.coverage.report]
precision = 2
skip_empty = true
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
    "@abc.abstractmethod",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "F811", "F401", "UP",
    "F841",  # local variable assigned but never used
    "B018",  # useless expression
  ]
ignore = ["E501"]
